apiVersion: v1
kind: Secret
metadata:
  name: skill-radar-secrets
  namespace: skill-radar-dev
type: Opaque
stringData:
  # Different auth secret so dev cookies don't conflict with prod
  BETTER_AUTH_SECRET: "REPLACE_WITH_DEV_SECRET"
  ANTHROPIC_API_KEY: "REPLACE_WITH_API_KEY"
