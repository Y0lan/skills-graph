apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: skill-radar-dev-data
  namespace: public-webapp-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
