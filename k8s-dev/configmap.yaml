apiVersion: v1
kind: ConfigMap
metadata:
  name: skill-radar-config
  namespace: skill-radar-dev
data:
  NODE_ENV: "production"
  PORT: "8080"
  DATA_DIR: "/data"
  CORS_ORIGIN: "https://dev.radar.sinapse.nc"
  BETTER_AUTH_URL: "https://dev.radar.sinapse.nc"
  LITESTREAM_BUCKET: "skill-radar-litestream-premier"
  LITESTREAM_PATH: "dev/ratings"
  CLAMAV_HOST: "localhost"
  CLAMAV_PORT: "3310"
