apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: skill-radar-dev-gateway
  namespace: skill-radar-dev
  annotations:
    networking.gke.io/certmap: skill-radar-dev-map
spec:
  gatewayClassName: gke-l7-global-external-managed
  listeners:
    - name: https
      protocol: HTTPS
      port: 443
    - name: http
      protocol: HTTP
      port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: skill-radar-dev-route
  namespace: skill-radar-dev
spec:
  parentRefs:
    - name: skill-radar-dev-gateway
      sectionName: http
    - name: skill-radar-dev-gateway
      sectionName: https
  hostnames: ["dev.radar.sinapse.nc"]
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: skill-radar
          port: 80
