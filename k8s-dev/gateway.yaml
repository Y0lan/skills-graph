apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: skill-radar-dev-route
  namespace: skill-radar-dev
spec:
  parentRefs:
    - name: cloud-sinapse-gateway
      namespace: gateway
      sectionName: https
  hostnames: ["dev.radar.sinapse.nc"]
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: skill-radar
          port: 80
