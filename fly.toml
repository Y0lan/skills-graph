app = "radar-competences"
primary_region = "cdg"

[build]

[env]
  NODE_ENV = "production"
  PORT = "8080"
  DATA_DIR = "/data"

[http_service]
  internal_port = 8080
  force_https = true

[mounts]
  source = "sqlite_data"
  destination = "/data"
