apiVersion: v1
kind: ConfigMap
metadata:
  name: skill-radar-config
  namespace: public-webapp
data:
  NODE_ENV: "production"
  PORT: "8080"
  DATA_DIR: "/data"
  CORS_ORIGIN: "https://radar.sinapse.nc"
  BETTER_AUTH_URL: "https://radar.sinapse.nc"
  LITESTREAM_BUCKET: "skill-radar-litestream-premier"
  LITESTREAM_PATH: "prod/ratings"
