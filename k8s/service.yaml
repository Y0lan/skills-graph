apiVersion: v1
kind: Service
metadata:
  name: skill-radar
  namespace: public-webapp
  labels:
    app: skill-radar
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: skill-radar
