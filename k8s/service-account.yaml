# Replace PROJECT_ID with your actual GCP project ID, or use kustomize to patch
apiVersion: v1
kind: ServiceAccount
metadata:
  name: skill-radar
  namespace: skill-radar
  annotations:
    iam.gke.io/gcp-service-account: skill-radar-runtime@sinapse-core.iam.gserviceaccount.com
