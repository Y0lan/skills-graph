#!/usr/bin/env node

console.error('seed-dev-compat.mjs was retired with the Postgres migration.')
console.error('Use the typed seed scripts through npm scripts against DATABASE_URL instead.')
process.exit(1)
